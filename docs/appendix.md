# Appendix

## Repository Structure

The repository includes both the analysis pipeline and a simple chat application for interacting with the retrieval system. The Python code handles indexing and search, while the Next.js app provides the user-facing interface.

## Reproducibility Notes

To keep the report reproducible, this appendix can later include package versions, commands used to build the search index, assumptions made during preprocessing, and any limitations of the available data.

## Future Additions

This section can also hold extended tables, prompt templates, annotation guidelines, or additional qualitative examples that are useful but too detailed for the main report narrative.
